apiVersion: v1
kind: Service
metadata:
  labels:
    name: pd 
  name: pd
  namespace: test4
spec:
  ports:
  - name: client
    port: 2379
    protocol: TCP
    targetPort: 2379
  selector:
    name: pd 
  sessionAffinity: None
  type: ClusterIP
